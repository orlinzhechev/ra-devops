# Dynamic configuration for ReconArt test environment
scrape_configs:
  - job_name: 'reconart_test'
    file_sd_configs:
      - files:
        - '/etc/prometheus/file_sd/reconart_test/*.yml'
        refresh_interval: 5m
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
