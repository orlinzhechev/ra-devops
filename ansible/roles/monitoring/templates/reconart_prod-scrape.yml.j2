# Dynamic configuration for ReconArt production environment
scrape_configs:
  - job_name: 'reconart_prod'
    file_sd_configs:
      - files:
        - '/etc/prometheus/file_sd/reconart_prod/*.yml'
        refresh_interval: 5m
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
